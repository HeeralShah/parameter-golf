# Mobile ↔ Codex smoke test

Created from ChatGPT via the GitHub integration.

## Purpose
This file is a minimal end-to-end test of the workflow:
1. Ideate or capture research on mobile in ChatGPT
2. Push structured notes into the repo
3. Return to laptop and let Codex read those notes

## Suggested laptop test
From your local clone of `parameter-golf`, try a prompt along the lines of:

```bash
codex "Read docs/mobile-codex-smoke-test.md and summarize the workflow in one paragraph"
```

## Next step ideas
- Add `docs/ideas.md` for raw thinking
- Add `docs/tasks.md` for actionable tasks
- Add `AGENTS.md` so Codex has stable project instructions
